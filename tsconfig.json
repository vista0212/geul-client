{
  "compilerOptions": {
    // 보통 import * as React from 'react'; 이런 식으로 가져오는데,
    // 아래 옵션을 사용하면 import React from 'react'; 형태로 가져올 수 있음.
    "esModuleInterop": true,
    // 아래 옵션 사용 시 에러난 위치를 찾아가기 편함.
    "sourceMap": true,
    // 라이브러리
    "lib": ["ES2020", "DOM"],
    // 리액트이기 때문에 jsx: "react"를 사용
    "jsx": "react",
    // 최신 모듈을 사용한다는 옵션
    "module": "esnext",
    // import / export 도 node가 해석할 수 있게 하겠다는 옵션
    "moduleResolution": "Node",
    //
    "target": "ES5",
    "strict": true,
    "resolveJsonModule": true,
    "baseUrl": ".",
    "paths": {}
  }
}